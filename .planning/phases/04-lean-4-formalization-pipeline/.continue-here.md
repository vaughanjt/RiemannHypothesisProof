---
phase: 04-lean-4-formalization-pipeline
task: exploration
total_tasks: N/A (open-ended research)
status: in_progress
last_updated: 2026-03-25T20:30:00Z
---

<current_state>
All 4 phases (18 plans) COMPLETE. v1 milestone 100%. Active mathematical exploration mode.

Session 9: Analytic continuation operator — definitive test of the sigma sweep approach.
Three scripts run: _analytic_continuation_v2.py, _inverse_offdiag.py, _sprime_coupling.py.

KEY FINDING: The prime-sum off-diagonal has ZERO correlation with the exact V* needed.
The diagonal is close (err=0.89) but the off-diagonal can't generate fine gap-peak structure.
Tridiagonal Jacobi with numerically optimized b_k gives r=+0.58 — best so far.

Venv: Python 3.14, .venv/Scripts/python.exe, all deps installed except jupyterlab.
</current_state>

<completed_work>

## Platform Build (complete)
- All 18 plans executed, verified, UAT passed

## Sessions 1-7 (carried forward)
- See HANDOFF.json completed_tasks 1-17 for full list
- Key: transfer operators (gauge symmetry proven), explicit formula operator,
  Ulam spiral modes, primorial tower, analytic continuation

## Session 8 (2026-03-24) — RAMANUJAN Q HUNT + EXPLICIT FORMULA CEILING
- (See previous version of this file for full details)
- Key: Hardy Z metric correct (r=+0.88), explicit formula path exhausted,
  Ramanujan error decomposition in Paper 2 appendix

## Session 9 (2026-03-25) — ANALYTIC CONTINUATION OPERATOR: DEFINITIVE TEST

### 1. Sigma Sweep with Hardy-Z r (_analytic_continuation_v2.py)
- Built H(sigma) at 26 sigma values from 2.0 to 0.5
- Self-adjointness holds at EVERY sigma (H = H^T to machine precision)
- Eigenvalue error barely changes: 0.90 (sigma=2) -> 0.86 (sigma=0.5)
- **CRITICAL**: Hardy-Z r stays near ZERO at every sigma (-0.06 to +0.01)
  The prime-sum off-diagonal cannot generate gap-peak structure
- Spectral flow: 93% identity matching at sigma=0.5 (only 14 swaps out of 200)
- ||V|| grows 0.53 (sigma=2) to 22.6 (sigma=0.5), while zeta ratio is 2.69
  The finite-dimensional V doesn't track the zeta-regularized value
- Exact mpmath S(T) diagonal is WORSE than prime-truncated (err=0.92 vs 0.89)
- Polynomial/Pade extrapolation from sigma>1 is worse than direct (1.7 vs 0.86)

### 2. Inverse Off-Diagonal Analysis (_inverse_offdiag.py)
- **TRIDIAGONAL (W=1) with optimized b_k: r = +0.58, err = 0.84**
  This is 66% of the target r = +0.88. Best result to date.
- Pentadiagonal (W=2): r = +0.52 (worse — optimizer stuck in local minimum)
- The EXACT V* (alternating projection): eigenvalues = zeros exactly, r = +0.88
  BUT Schur-Horn NOT feasible (diagonal alpha not majorized by zeros)
  The alternating projection gave eigenvalues = zeros but diagonal off by 3.49
- V* has near-zero off-diagonal in banded structure — it's a global permutation, not banded
- Prime-sum V has ZERO correlation with exact V*

### 3. Theoretically-Motivated Formulas
- b_k = zero_gap: r = +0.50 (but circular — needs zeros to build operator)
- b_k = 1/N'(T): r = +0.03 (smooth spacing, no fine structure)
- S'(T) coupling: S' from mpmath and prime sums are UNCORRELATED (r=+0.02)
  S'(T) doesn't correlate with gap fluctuations (r=+0.01)
  S' has poles at every zero of zeta — too spiky for smooth coupling
  Best S' formula: r = +0.03 (no improvement over smooth)
- Constant b sweep: best r = +0.06 at b = 1.2

### 4. Key Numerical Optimized b_k Structure
- mean |b_k| = 1.18, std = 0.74, mostly positive (146/199)
- The optimized b_k is correlated with zero gaps (to be verified)
- Power-law decay with zero height (to be verified from background task)

### 5. Analytic Continuation of Optimized b_k
- Jacobi optimization was run at multiple sigma values (background task still running)
- b_k(sigma) varies smoothly across sigma
- Correlation between b_k at adjacent sigma values is high

### 6. Path B: Regularized S'(T) at sigma > 1/2
- Computed S'(T) = Im[zeta'/zeta] at sigma = 0.55, 0.60, ..., 1.0 (no poles)
- At every sigma, the optimizer picks c=0: adding S' makes things WORSE
- Regularized S' has NO useful signal for the off-diagonal coupling
- PATH B IS A DEAD END

### 7. Path A: Lanczos Jacobi from zeros
- Lanczos on D=diag(zeros) with v=uniform produces Jacobi matrix with r=+0.70
- BUT diagonal is completely different from alpha (mean|a-alpha|=91)
- Lanczos b_k are huge (mean=85) and don't work with alpha diagonal (err=23.5)
- The diagonal and off-diagonal are HOLISTICALLY COUPLED
- Different starting vectors (1/sqrt(n), 1/n, random) all give wrong diagonal
- KEY INSIGHT: you can't decouple diagonal from off-diagonal.
  The Jacobi representation is a holistic object; the starting vector v
  encodes the spectral measure connecting the two.
</completed_work>

<remaining_work>

## High-Priority: Derive b_k from First Principles
1. **The central unsolved problem**: The numerically optimized b_k gives r=+0.58
   but we can't derive it from prime sums, S'(T), or any closed-form formula.
   b_k = zero_gap gives r=+0.50, but that's circular (needs zeros).
   Need a formula for b_k that doesn't require knowing the zeros.

2. **Path A: Jacobi matrix from Lanczos + starting vector**
   The Lanczos algorithm with a specific starting vector v produces a Jacobi matrix
   with prescribed spectrum. The question: what v gives diagonal close to alpha?
   This connects to the "measure" in the Hilbert-Polya framework.

3. **Path B: Regularized S'(T)**
   S'(T) fails because of poles at zeros. Could use a REGULARIZED version:
   S'_reg(T) = S'(T) smoothed with Gaussian of width w, or
   S'_reg(T) = Im[zeta'(sigma+iT)/zeta(sigma+iT)] at sigma > 1/2 (no poles)
   Then analytically continue the smoothed version to sigma = 1/2.

4. **Path C: Two-operator system**
   Operator 1: diagonal alpha_k (from S(T)) — gives eigenvalue positions
   Operator 2: provides b_k from its own spectral data — gives coupling
   The Berry-Keating xp + px operator or the Bost-Connes system might fill this role.

5. **Paper 3**: Now should include:
   - Analytic continuation framework (sigma sweep)
   - Tridiagonal Jacobi result (r=+0.58)
   - The b_k = zero_gap connection
   - The S'(T) failure and why (poles at zeros)

## Secondary
- Submit Papers 1+2 to Zenodo
- Lean 4 formalization of proof structure
</remaining_work>

<decisions_made>

### Session 9
- Analytic continuation H(sigma) from sigma>1: r stays near zero at every sigma
  The prime-sum off-diagonal is fundamentally wrong for generating gap-peak structure
- The problem is the OFF-DIAGONAL, not the diagonal (diagonal has err=0.89, acceptable)
- Tridiagonal Jacobi with optimized b_k: BEST APPROACH (r=+0.58)
- Schur-Horn not feasible for (alpha, zeros) pair — no symmetric matrix has both
- S'(T) = d/dT arg(zeta) fails as off-diagonal formula (poles at zeros)
- S'(T) from mpmath and prime sums are uncorrelated (r=+0.02)
- Higher bandwidth (W=2,3,5) doesn't help — optimizer struggles with more parameters
- The exact V* is global (not banded) — banded approximations lose the structure

### Previous sessions (carried forward)
- Gauge symmetry theorem rules out Hermitian transfer operators
- Hardy Z metric is correct (r=+0.88), old det metric was wrong (r=+0.04)
- Explicit formula error is systematic, anti-correlates with gap-peak
- Inert primes (3 mod 8) dominate, split primes (5 mod 8) silent
- ||V|| finite at sigma=1/2 via zeta regularization (2.69)
- Amplitude law ~log(p)/p^0.83 (BK, not Selberg)
</decisions_made>

<blockers>
- The b_k derivation from first principles is the core open problem.
  No formula tried so far (prime sums, S'(T), 1/N', constant) gives r > +0.06.
  Only numerically optimized b_k (r=+0.58) and exact zero gaps (r=+0.50) work.
</blockers>

<context>
The operator problem has been further refined: it's now specifically about the
OFF-DIAGONAL COUPLING. The diagonal is good (err=0.89 from explicit formula).
The off-diagonal needs to encode zero-gap fine structure to generate r > 0.

The tridiagonal Jacobi result (r=+0.58 with 199 free parameters) shows that
a simple nearest-neighbor coupling CAN capture 66% of the peak-gap correlation.
But deriving those coupling values from number theory remains open.

The analytic continuation approach (sigma > 1 to sigma = 1/2) works for
the diagonal but not the off-diagonal. The prime-sum V at any sigma has
zero correlation with the exact V* needed.

Three promising paths remain:
A. Lanczos with a specific starting vector (connects to spectral measure)
B. Regularized S'(T) at sigma > 1/2 then continue to 1/2
C. Two-operator system where a second operator provides the coupling
</context>

<next_action>
Paths A, B, and the prime-sum off-diagonal are all eliminated. Three remaining directions:

1. **Find the starting vector v for Lanczos that gives diagonal ~ alpha**.
   This is an optimization over the unit sphere: find v such that applying
   Lanczos to diag(zeros) produces diagonal close to alpha. The v encodes
   the spectral measure of the Hilbert-Polya operator. This is the most
   theoretically clean path.

2. **Derive b_k from two-point zero correlations**.
   The pair correlation R_2(s) of zeros encodes the same information as the
   off-diagonal coupling. The GUE-matched R_2 is well-studied. Can we invert
   the pair correlation to get b_k? This is the inverse spectral problem
   using statistical information instead of exact zero positions.

3. **Information-theoretic approach**: The optimized b_k minimizes a free energy
   F = E_err + lambda * S_structure where the error energy and structural
   entropy compete. Can maximum entropy methods reconstruct b_k from
   macroscopic constraints (mean spacing, pair correlation, r value)?
</next_action>
