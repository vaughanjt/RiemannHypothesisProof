---
phase: research-session-40
task: 0
total_tasks: 0
status: paused
last_updated: 2026-04-03T02:52:38.967Z
---

<current_state>
Session 40 COMPLETE. Five-phase breakthrough session. Hodge approach
killed (trivially satisfied), barrier constant discovered on range(W02),
RH reduced to determinant inequality on 4x4 effective matrix.
</current_state>

<completed_work>

**Phase 1:** Built session40_hodge_explorer.py (Dash app with slider/prime toggles)
and session40_star_optimizer.py (Riemannian GD on O(44)/U(22) manifold).

**Phase 2:** Proved {J,[J,M]}=0 algebraic identity. Eigenvalues of [J,M] in exact
+-lambda pairs. Definiteness impossible on full space. Optimizer correctly -> 0.

**Phase 3:** Killed Hodge-Riemann: M is neg-def on null(W02), so Q_W = -M > 0
trivially. Lefschetz search (12 candidates + optimization) found no definite
subspace > dim 2. Wrong mountain entirely.

**Phase 4:** Discovered constant barrier ~0.04 on range(W02).
W02 = pf*(L^2*u*u^T - (4pi)^2*w*w^T), exact rank-2 decomposition.
Both W02 and M grow as lam^1.5 but Q_W stays ~0.04.
Barrier = (W02-M_prime) - M_diag - M_alpha = 2.69 - 1.61 - 1.04 = 0.04.
Three log-slopes cancel: 0.257 - 0.193 - 0.073 = -0.009.
2x2 block is perfectly diagonal (parity, M_12=0 exactly).

**Phase 5:** 4x4 effective matrix (range + 2 coupled null directions).
Decouples by parity into two 2x2 blocks.
RH <=> det > 0 <=> QW_barrier * QW_null_coupled > M_cross^2.
At lam^2=10000: 0.032*9.354 > 0.519^2 -> 0.299 > 0.269. Margin 11%.
</completed_work>

<remaining_work>

1. PROVE QW_barrier -> c > 0 from Weil explicit formula (the provable part)
2. Track the 4 determinant quantities vs lambda^2 at scale
3. Derive closed-form asymptotics for each barrier component
4. Understand WHY 3 log-slopes cancel (0.257 - 0.193 - 0.073 = -0.009)
5. Investigate silent modes (M ~ -9e-9 on null) staying negative
6. Cross-coupling: norm 0.5 but effect 10^-10 — prove this persists
</remaining_work>

<decisions_made>
- Hodge-Riemann is trivially satisfied on null(W02) — M neg-def there
- {J,[J,M]}=0 kills [J,M] definiteness — algebraic, not fixable
- Real fight is on 2-dim range(W02), not 44-dim null
- RH reduces to determinant condition: 4 explicit Weil formula sums
- Barrier ~0.04 is close to 1/(8*pi)=0.0398 but not exact match
</decisions_made>

<blockers>
- Numerical precision limits eps_0 at ~10^-10 for large lambda^2
- Cross-coupling cancellation (norm 0.5 -> effect 10^-10) needs analytical proof
</blockers>

<context>
Most productive session in the project. Each dead end led directly to the next
insight. The barrier constant is the key target: if proved, it establishes the
hardest piece of the determinant inequality. The four numbers (QW_barrier,
QW_null_coupled, M_cross, det) are each explicit sums from the Weil formula
involving the odd eigenvector w[n] = n/(L^2 + (4*pi)^2*n^2).
</context>

<next_action>
/gsd:resume-work -> prove the barrier constant analytically.
Each component (<w,W02,w>, <w,M_prime,w>, <w,M_diag,w>, <w,M_alpha,w>)
has a closed-form expression. Derive asymptotics as L -> infinity.
</next_action>
